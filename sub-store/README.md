# sub-store

Sub-Store 订阅管理服务。

## 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `SUB_STORE_PORT` | `13003` | Web 端口 |
| `SUB_STORE_BACKEND_PATH` | `/Ku91677Gk7fHKlMQagFl` | 后端 API 路径 |

## 启动

```bash
make up-sub-store
# 或
cd sub-store && docker compose up -d
```
