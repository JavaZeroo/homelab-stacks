# ─── Environment files (contain secrets) ──────────────
# 忽略所有 .env（根目录和各 stack 目录下），但保留 .env.example
.env
**/.env
!.env.example

# ─── Basic-auth 密码文件（含哈希，勿提交）─────────────
**/.htpasswd

# ─── OS / Editor ──────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/

# ─── Python / Build ──────────────────────────────────
.venv/
__pycache__/

# ─── Docker runtime data ─────────────────────────────
*/volumes/
*/backup/


# ─── Valheim: game saves, backups, server binaries ───
valheim-server/config/
valheim-server/data/

# ─── 约定 ────────────────────────────────────────────
# 本仓库只存放 Compose 编排与配置，不提交运行数据。
# 生产数据、密钥统一在 /data/apps 下（不进 Git）。

# ─── Sub-Store 运行数据（含订阅密钥，勿提交）─────────────
sub-store/data/
